{
    "require-dev": {
        "phpunit/phpunit": "^9.6",
        "wp-phpunit/wp-phpunit": "^6.0",
        "yoast/phpunit-polyfills": "^2.0"
    }, 
    "scripts": {
        "test": "phpunit",
        "test:host": "WP_CORE_DIR=${WP_CORE_DIR:?Set_WP_CORE_DIR} phpunit",
        "test:docker": "docker compose run --rm wp-tests"
    }
}
